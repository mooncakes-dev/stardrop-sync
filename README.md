# Setup

**Step 1**: Make sure you have [Rust](https://www.rust-lang.org/learn/get-started) installed locally.

**Step 2**: Run `npm install` in the project to install all of the dependencies.

**Step 3**: Run `npm run tauri dev` to run the Tauri app.